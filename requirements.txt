# Core
transformers>=4.45.0
trl>=0.15.0
peft>=0.11.0
accelerate>=0.28.0
datasets>=2.18.0
tensorboard>=2.14.0
einops>=0.7.0

# Optionnel (utile en local/serveur, ignoré sur Kaggle si non utilisé)
# vllm>=0.5.0
# bitsandbytes>=0.43.0
